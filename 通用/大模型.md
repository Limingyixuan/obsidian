### 词的向量化：
为何要把词向量化表示：
将词语进行向量化表达就是将词语的各个性质抽象出来，以具体数字的形式加以量化，使得词语之间可以互相比较来判断远近关系。

### 位置编码的方法：
有三种
绝对位置编码，相对位置编码，旋转位置编码

#### 1、绝对位置编码：
##### 可学习式的
##### 三角式位置编码：

![](Pasted%20image%2020250723153804.png)

pos相当于是每个token在整个序列中的位置，相当于是0, 1, 2, 3...(看序列长度是多大，比如10，比如100)，
d_model 代表位置向量的维度(也是词embedding的维度，transformer论文中设置的512维)
至于 i 是embedding向量的位置下标对2求商并取整，它的取值范围是

![](Pasted%20image%2020250723213656.png)

