
1、batch norm and layer norm



### 基于 8 连接的区域生长 完整执行步骤

这是算法的**标准化流程**，严格按步骤执行即可实现分割：

#### 步骤 1：图像预处理

输入灰度图像，可选去噪（高斯滤波），减少噪声对生长的干扰。

#### 步骤 2：初始化参数

- 创建**标记矩阵**：和原图大小相同，初始全为 0（`0=未访问`，`1=已归入目标区域`）；
- 选定**种子点 `(sx, sy)`**，将标记矩阵对应位置设为 1；
- 定义**灰度阈值 T**（控制生长的严格程度）；
- 初始化**队列 / 栈**：存储待检查的像素（种子点先入队）。

#### 步骤 3：8 邻域遍历与生长

循环处理队列中的像素，直到队列为空：

1. 取出队列**第一个像素 `(x, y)`**；
2. 遍历该像素的**8 个邻域像素 `(nx, ny)`**；
3. 对每个邻域像素做**三重校验**：
    
    ✔ 坐标不越界（在图像范围内）；
    
    ✔ 未被访问（标记矩阵 = 0）；
    
    ✔ 灰度差 ≤ 阈值 T；
4. 满足所有条件：标记为 1（归入区域），并入队列等待后续检查。

### 步骤 4：输出结果

标记矩阵中值为 1 的像素，就是**分割完成的目标区域**。


### 图像对齐的方法：
一、单应性矩阵：
适合 平面+只有旋转（不同角度），没有平移

求单应性矩阵：
1、用特征点算法：
ORB / SIFT / SURF
2、暴力匹配 / FLANN 匹配
3、## 鲁棒求解 H（必须用！）
**RANSAC 算法**
作用：剔除**误匹配点（外点）**，得到正确稳定的 H。
不用 RANSAC → H 基本都是错的。



### 图像增强
方法有：
基于像素的点处理：
1、线性灰度变换：线性拉伸
2、非线性灰度变换：对数变换、幂次变换、直方图均衡化

空域方法（像素领域）
1、基于单个像素的点处理
2、基于像素邻域的空域滤波

空域方法和频域方法：

空域平滑滤波 -----频域低通滤波
空域锐化滤波------频域高通滤波



## 卷积模板

1均值卷积：
![](image/Pasted%20image%2020260324133728.png)
适配任务：
高斯噪声去除
图像降采样
卷积操作的教学演示



![](image/Pasted%20image%2020260324143033.png)

锐化卷积：
![](image/Pasted%20image%2020260324143108.png)


拉普拉斯卷积核：
![](image/Pasted%20image%2020260324143503.png)

边缘检测卷积核：

sobel卷积核：
![](image/Pasted%20image%2020260324143624.png)

