## 动态规划和多维动态规划：




## 图论：

做法为：
1、将题目翻译为图，
找出谁是节点谁是边

2、选存储方式：
稀疏图：邻接表
稠密图：邻接矩阵

3、套用对应算法：
- 找连通、遍历 → DFS / BFS
- 依赖关系、排顺序 → 拓扑排序
- 两点最短距离 → 最短路径
- 连通性、合并集合 → 并查集

4、加标记防重复



### 技巧：
#### bfs
按时间扩散的应该使用bfs：bfs会用到队列


#### 拓补排序：
1、判断有向图是否有环用拓补排序



- 构建有向图的邻接表，同时统计每个节点的「入度」（有多少条边指向该节点，即该课程有多少先修课）；
- 初始化队列：将入度为 0 的节点（无先修课的课程）加入队列；
- BFS 遍历：
    
    - 取出队首节点，统计已处理的课程数；
    - 遍历该节点的所有邻接节点，将其入度减 1（相当于完成先修课后，后续课程的先修要求减少一个）；
    - 若邻接节点入度变为 0，加入队列；
    
- 最终判断：已处理的课程数是否等于总课程数（等于→无环，返回 true；否则→有环，返回 false）。


