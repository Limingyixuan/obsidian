
### 1、策略梯度

策略梯度定理（Policy Gradient Theorem）是强化学习中的一个重要理论结果，它为直接优化策略提供了一种数学框架。而策略梯度方法通过直接对策略参数进行梯度上升来优化策略，从而最大化期望回报。在强化学习中，策略（Policy）是一个从状态到动作的映射，通常表示为 π(a∣s;θ)π(a∣s;θ) ， θθ 是需要学习的某个参数。策略梯度方法的目标是找到一组参数 θθ ，使得策略在环境中执行时能够最大化累积回报，即：

![](image/Pasted%20image%2020260401174132.png)
轨迹序列 ττ 是在使用策略 πθπθ​ 的情况下采样出来的，由于策略和状态转移都有随机性， τ∼πθτ∼πθ​ 隐藏的完整含义为：
 
 ![](image/Pasted%20image%2020260401174140.png)

